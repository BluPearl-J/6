temperatures.items() returns every  key–value pair in the dictionary.

k is  dayo (e.g., 'Monday'), and v is the list of temperatures for that day.

sum(v)/len(v) computes the average temperature.

:.2f formats the result to two decimal places.
